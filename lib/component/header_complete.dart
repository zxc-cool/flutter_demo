import 'package:flutter/material.dart';

class HeaderComplete extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        '加载成功',
        style: TextStyle(color: Color(0xFF8F929B),fontSize: 14.0),
      ),
    );
  }
}
